# Posts

Drop rendered HTML exports from `.qmd` files here, one folder or file per post
(e.g. `posts/my-post-title.html`, or `posts/my-post-title/index.html` if a post
has its own assets like figures).

Once there's real content, `blog/index.html` should be updated to list posts
here instead of showing "Coming soon."
